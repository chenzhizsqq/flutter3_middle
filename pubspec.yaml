name: helloworld
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.19.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.4.0
  shared_preferences: ^2.0.18
  http: ^0.13.0

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.2

  #firebase
  firebase_core: ^2.15.1
  firebase_auth: ^4.7.3
  cloud_firestore: ^4.4.5
  firebase_database: ^10.0.16

  #flutter_riverpod 
  #flutter_riverpod: 是 provider 的进化版本，它提供了更多的功能和特性，包括更强大的依赖注入、更灵活的作用域管理、更方便的测试等。
  #它的设计更加灵活，适合处理更复杂的应用程序需求。
  flutter_riverpod: ^2.4.9
  riverpod_annotation: ^2.3.3

  json_annotation: ^4.8.1
  
  #GetX
  get: ^4.6.5   

  #provider
  #provider 是一个 Flutter 中非常流行的状态管理库，它提供了简单而强大的工具来管理应用程序的状态。
  provider: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.2
  build_runner: ^2.3.3
  freezed: ^2.3.2

  json_serializable: ^6.8.0 # json处理库

  #flutter_riverpod
  custom_lint:
  riverpod_generator: ^2.3.9
  riverpod_lint: ^2.3.7

flutter:
  uses-material-design: true
  # assets:
  #  - images/a_dot_burr.jpeg
  #  - images/a_dot_ham.jpeg
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
